#!/bin/bash
pwd
cd ./img

for f in * 
do
	if [ $f ~= "PLACEHOLDER.txt" ]
	then
		testo="
		\begin{figure}
		 \centering 
		 \includegraphics[width=\textwidth]{${f}}
		 \caption{$(basename $f)} 
		 \label{fig:${f}}
		\end{figure}
		"
	fi
done

echo $testo > ../latex/img/${f}

exit 0;
